name: neuroline
description: A sample command-line application.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  path: ^1.7.0
  image: ^2.1.13
  perceptron:
    git:
      url: https://github.com/izolenta/perceptron.git

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
